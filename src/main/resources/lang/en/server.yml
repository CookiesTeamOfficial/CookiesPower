# Server MOTD settings
server-motd:
  # Configuration for normal server state
  normal:
    motd:
      # Enable MOTD
      enable: true
      # Use random text from the list
      random: true
      # Update MOTD when the server is pinged or based on a time interval. (true - update on ping, false - update at the specified interval (update-interval))
      update-when-ping: true
      # Time interval for updating the MOTD when update-when-ping is set to false
      update-interval: 1s
      # MOTD text
      texts:
        - "&fThis server use &dCookiesPower&r! \n&fBecause this server cool!"
        - "&fThis server use &aCookiesPower&r! \n&fBecause this server cool!"
        - "&fThis server use &cCookiesPower&r! \n&fBecause this server cool!"
        - "&fThis server use &4CookiesPower&r! \n&fBecause this server cool!"
        - "&fThis server use &1CookiesPower&r! \n&fBecause this server cool!"
        - "&fThis server use &9CookiesPower&r! \n&fBecause this server cool!"
        - "&fThis server use &bCookiesPower&r! \n&fBecause this server cool!"
        - "&fThis server use &2CookiesPower&r! \n&fBecause this server cool!"
        - "&fThis server use &3CookiesPower&r! \n&fBecause this server cool!"
        - "&fThis server use &cCookiesPower&r! \n&fBecause this server cool!"

    # Icon's settings
    icons:
      # Enable icons change
      enable: true
      # Use random icons from the list
      random: true
      # List of icons
      # - You can specify file names if the icons are located in the 'icons' folder (e.g., server-icon-1.png).
      # - Alternatively, you can provide direct URLs to use icons hosted online.
      icons-list:
        - "server-icon-1.png"
        - "server-icon-2.png"
        - "server-icon-3.png"
        - "server-icon-4.png"
        - "server-icon-5.png"

    # Max players settings
    max-players:
      # Enable max players display
      enable: true
      # Max player count
      count: 100

    # Fake players settings
    fake-players:
      # Enable fake players
      enable: false
      # Fake players count (if enabled)
      count: 31
      # Generate random names for fake players
      generate-random-names: true

    # Version settings
    version:
      # Enable version display
      enable: true
      # Use random text from the list
      random: true
      # Protocol, see - https://wiki.vg/Protocol_version_numbers | -1 means custom text
      protocol: -1
      # MOTD text
      texts:
        - "&dCookiesPower"
        - "&aCookiesPower"
        - "&eCookiesPower"
        - "&cCookiesPower"
        - "&3CookiesPower"

  # Configuration for full server state (joined max. players)
  server-full:
    motd:
      # Enable MOTD
      enable: true
      # Use random text from the list
      random: true
      # Update MOTD when the server is pinged or based on a time interval. (true - update on ping, false - update at the specified interval (update-time))
      update-when-ping: true
      # Time interval for updating the MOTD when update-when-ping is set to false
      update-interval: 1s
      # MOTD text
      texts:
        - "&fThis server use &6CookiesPower&r! \n&eBut server is full! Try join later!"

    # Icon's settings
    icons:
      # Enable icons change
      enable: true
      # Use random icons from the list
      random: true
      # List of icons
      # - You can specify file names if the icons are located in the 'icons' folder (e.g., server-icon-1.png).
      # - Alternatively, you can provide direct URLs to use icons hosted online.
      icons-list:
        - "server-icon-1.png"
        - "server-icon-2.png"
        - "server-icon-3.png"
        - "server-icon-4.png"
        - "server-icon-5.png"

    # Max players settings
    max-players:
      # Enable max players display
      enable: true
      # Max player count
      count: 100

    # Fake players settings
    fake-players:
      # Enable fake players
      enable: false
      # Fake players count (if enabled)
      count: 31
      # Generate random names for fake players
      generate-random-names: true

    # Version settings
    version:
      # Enable version display
      enable: true
      # Use random text from the list
      random: true
      # Protocol, see - https://wiki.vg/Protocol_version_numbers | -1 means custom text
      protocol: -1
      # MOTD text
      texts:
        - "&fServer currently full!"

# Server brand settings
server-brand:
  # Enable server brand change in F3
  enable: true
  # Use random text from the list
  random: false
  # Server brand update time
  update-interval: 1s
  # MOTD text
  texts:
    - "&dCookiesPower"
    - "&aCookiesPower"
    - "&eCookiesPower"
    - "&cCookiesPower"
    - "&3CookiesPower"
